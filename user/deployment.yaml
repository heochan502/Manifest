apiVersion: apps/v1
kind: Deployment
metadata:
  name: otd-user-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otd-user
  template:
    metadata:
      labels:
        app: otd-user
    spec:
      containers:
      - name: otd-user
        image: harbor.greenart.n-e.kr/otd/user:2
        ports:
          - containerPort: 8080
        volumeMounts:
          - name: host-volume
            mountPath: /home/download
      volumes:
        - name: host-volume
          hostPath:
            path: /home/green/download
            type: DirectoryOrCreate      
      imagePullSecrets:
      - name: harbor-creds
      serviceAccountName: spring-gateway