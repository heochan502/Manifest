apiVersion: v1
kind: Service
metadata:
  name: otd-user
  namespace: app
  labels:
    app: otd-user
spec:
  selector:
    app: otd-user
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP